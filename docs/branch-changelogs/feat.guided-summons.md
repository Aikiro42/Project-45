# New (modders)
- Added the 'onSpawnProjectile(projectileId)' interface for passive mods. This interface only matters for Projectile or Summoned projectiles.
- Added 'preserveAimAngle' to summoned projectile parameters, which sets the summoned projectile's rotation to the owner's aim angle instead of a random angle.