# New
- Added the **Flashlight**, which is basically like the Laser Beam but flashlight.

# New (Modders)
- Added the 'flashlight' parameter, which is similar to but simpler than the 'laser' parameter:
```
"flashlight: {
  "color": [255, 255, 255],
  "enabled": true
}
```